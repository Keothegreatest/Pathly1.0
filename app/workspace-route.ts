const tabs:Record<string,string[]>={Home:[],Experiences:['experience','reflection','story','timeline','analytics','capture'],Schools:['school','requirement','comparison'],Application:['readiness','experience','letter','writing','tasks'],Goals:[]};
export function workspaceRoute(search:string){const params=new URLSearchParams(search);const requested=params.get('view')||'Home';const page=Object.hasOwn(tabs,requested)?requested:'Home';const choices=tabs[page];const requestedTab=params.get('tab')||'';return {page,tab:choices.includes(requestedTab)?requestedTab:choices[0]||'experience'};}
