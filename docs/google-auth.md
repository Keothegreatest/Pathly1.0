# Authentication removed

Pathly now asks for a preferred name and stores the visitor's records locally in their browser. No Google OAuth setup is needed. See the main README for storage and privacy behavior. Existing D1 records and auth tables are not deleted or exposed to visitors.
